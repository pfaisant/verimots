{
  "name": "verimots",
  "version": "4.0.2",
  "private": true,
  "type": "module",
  "description": "Verimots — French word checker, rack search and daily challenge. Web + native Android.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "serve": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "apk": "bash scripts/build-apk.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "google-auth-library": "^11.0.2"
  },
  "devDependencies": {
    "puppeteer-core": "^25.8.0"
  }
}
